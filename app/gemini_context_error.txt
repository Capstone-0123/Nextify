지금 증상은 단순 Gemini 응답 실패라기보다, Windows에서 --prompt-interactive로 너무 긴 문자열을 명령줄 인자로 넘겨서 깨지는 패턴에 가깝습니다.

핵심 근거는 여기입니다.


ai-review-session.cjs
Lines 184-195
async function runAiReviewSessionCliStream(opts) {
  const {
    sessionPath,
    signal,
    onChunk,
    command = 'gemini',
    args = [],
    mode = 'interactive-seeded',
    workingDirectory,
    model,
그리고 interactive-seeded일 때 seed prompt를 명령줄 인자로 넣고 있습니다.


ai-review-session.cjs
Lines 291-295
            const effectiveArgs =
              normalizedMode === 'interactive-seeded' && interactiveSeedPrompt
                ? [...effectiveArgsBase, '--prompt-interactive', toSingleLinePromptArg(interactiveSeedPrompt)]
                : effectiveArgsBase;
문제는 이 interactiveSeedPrompt가 파일 변경 메타데이터를 전부 합쳐서 만들고 있다는 점입니다.


ai-review-session.cjs
Lines 19-31
function buildInteractiveSeedPrompt(session, sessionPath) {
  const step = session?.step || 'unknown';
  const changes = Array.isArray(session?.changes) ? session.changes : [];
  const compactChanges = changes
    .map((c, idx) => {
      const id = c?.id || `idx-${idx + 1}`;
      const rel = c?.relativePath || '(unknown)';
      const type = c?.type || 'unknown';
      const before = c?.diffBeforePath || '(none)';
      const after = c?.diffAfterPath || '(none)';
      return `- id=${id}, path=${rel}, type=${type}, diffBeforePath=${before}, diffAfterPath=${after}`;
    })
    .join('\n');
즉 지금은 37개 변경에 대해

파일 경로
타입
before path
after path
를 전부 붙여서 하나의 긴 프롬프트로 만든 뒤, 그걸 다시 한 줄로 압축해서:

gemini --prompt-interactive "엄청 긴 문자열..."
형태로 넘기고 있습니다.


ai-review-session.cjs
Lines 57-62
function toSingleLinePromptArg(text) {
  return String(text || '')
    .replace(/\s+/g, ' ')
    .replace(/"/g, "'")
    .trim();
}
여기서 Windows는 특히 취약합니다.
왜 Windows에서 더 잘 터지나
현재 Windows에서는 cmd.exe /c를 거쳐 실행하고 있습니다.


ai-review-session.cjs
Lines 301-310

            if (isWin) {
              child = spawn('cmd.exe', ['/d', '/s', '/c', candidate, ...effectiveArgs], {
                stdio,
                cwd: workingDirectory,
                windowsHide: true,
                shell: false,
              });
            }
이 경우 실질적으로는 cmd.exe 명령줄 길이 제한 영향을 강하게 받습니다.
그래서 메시지처럼:

명령줄이 너무 깁니다
Gemini CLI review failed (code=1, signal=none)
가 같이 뜨는 건 주입량이 많아서일 가능성이 큽니다.

결론
질문에 바로 답하면:

네, 거의 맞습니다. 지금 에러는 diff가 37개라서 seed prompt에 들어가는 메타데이터가 길어졌고, 그걸 Windows 명령줄 인자로 넘기면서 길이 제한에 걸린 상황일 가능성이 높습니다.

지금 구조에서 특히 긴 부분
길이를 키우는 요소는 이 셋입니다.

변경 파일 개수 자체
지금 37개 변경
각 파일마다 들어가는 정보량
id
relativePath
type
diffBeforePath
diffAfterPath
Windows 실행 방식
cmd.exe /c gemini ... --prompt-interactive "...긴 문자열..."
어떻게 줄이는 게 맞나
수정 방향은 보통 3가지입니다.

interactive-seeded를 포기하고 plain interactive로만 실행
가장 단순
명령줄 길이 문제 거의 사라짐
대신 자동 컨텍스트 주입이 약해짐
seed prompt를 아주 짧게 줄이기
파일 전체 목록 대신:
총 변경 개수
세션 파일 경로
변경 타입 요약
상위 몇 개 파일만
이 방식이면 현재 구조 유지 가능
Windows에서는 --prompt-interactive를 쓰지 않고 fallback
예:
Windows + 변경 수 많음 -> plain interactive
macOS/Linux -> interactive-seeded 유지
가장 현실적일 수 있음
추천 판단
현재 요구사항이

프롬프트 내용은 사용자에게 안 보이게
터미널 출력은 짧게
최종 리뷰 때 한 번만 Gemini 대화
이 방향이라면, 사실 2번 또는 3번이 제일 맞습니다.

특히 안전하게 가려면:

변경 수가 많을 때는 interactive-seeded를 끄고
Gemini는 plain interactive로 띄우고
사용자는 session.json이나 review panel 기준으로 질문
하는 쪽이 덜 깨집니다.

발표나 설명용 한 줄
“최종 리뷰 단계에서 변경 파일 수가 많아지면서, Gemini CLI에 넘기는 --prompt-interactive 인자 길이가 Windows 명령줄 제한을 초과한 것으로 보입니다. 즉 diff 수가 많아져서 prompt seed 메타데이터가 과도하게 길어진 것이 원인일 가능성이 높습니다.”

원하시면 다음 답변에서 바로

가장 안전한 수정안 2개
또는 이 문제를 해결하는 최소 변경 계획 으로 정리해드릴게요.