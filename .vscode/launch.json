{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Nextify Review Extension",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/app/extension"
      ]
    }
  ]
}